# Используем базовый образ для компиляции C++ приложений
FROM gcc:latest

# Устанавливаем рабочую директорию в контейнере
WORKDIR /app

# Копируем исходные файлы в контейнер
COPY main.cpp .

# Компилируем приложение
RUN g++ -o helloWorld main.cpp

# Указываем команду для запуска программы
CMD ["./helloWorld"]
